#include <stdio.h>

int main() {
    int n;
    int a;
    scanf("%i %i", &n, &a);
    int avant = -1;
    for(int i = 0; i < n; ++i)
    {
        if(i != 0)
        {

        }
    }
    return 0;
}